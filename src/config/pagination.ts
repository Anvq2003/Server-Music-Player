// pageSize?: number;

// current?: number = 1;

// total?: number = undefined;

// hasNext?: boolean;

// hasPrevious?: boolean;

export const pagination = {
  totalDocs: 'totalData',
  docs: 'data',
  limit: 'pageSize',
  page: 'current',
  nextPage: 'next',
  prevPage: 'prev',
  totalPages: 'total',
  pagingCounter: 'slNo',
  meta: 'paginator',
};
